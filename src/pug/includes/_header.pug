include ../mixins/_mixins
header
    .b-header__top
        .b-top.content_middlemenu
            .b-top__logo
                .b-logo
                    img.b-logo__img(src="img/logo_top.png")
            -var menu=[{href: 'Index', title: 'Home'}, {href:'insidepage', title: 'About us'}, {href:'blog', title: 'Blog'}]
            nav.b-headermenu
                ul
                    each item in menu
                        li
                            if item.title.toLowerCase() === currentPage
                                span.current-page= item.title
                            else
                                a(href=item.href.toLowerCase().replace("\ ", "-")+".html")= item.title
    .b-header__inform
        .b-inform.content_middle
            .b-inform
                .b-inform__about
                    .b-about
                        img.b-about__img(src="img/logo.png")
                        h2.b-about__slogan The Forest is is a large area with trees or other woody vegetation.
                                .b-about__line
                        p.b-about__text Forests at different latitudes form distinctly different ecozones: boreal forests near the poles tend to consist of evergreens, while tropical forests near the equator tend to be distinct from the temperate forests.mid-latitude.
                        .b-about__btn
                            a.b-btn__load(href="#") download
                block formheader
                    include ../includes/_formheader